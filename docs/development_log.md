# Development Log

## 日付

2026-07-23

## 実装した内容

- 第1段階のディレクトリと設定・取得・可視化・UIを作成
- トヨタ自動車（7203.T）の過去1年の日足、20日・50日移動平均に対応
- 空データ、列不足、通信失敗、MultiIndex列への対処を実装
- 通信に依存しないpytestを作成

## 発生した問題

- 制限された実行環境内では、PyPIとYahoo Financeの名前解決ができなかった。
- Apple標準Python 3.9のLibreSSL 2.8.3に対し、urllib3 v2の互換性警告が出た。
- 制限された実行環境内では、Streamlitがローカルポートをbindできなかった。
- Streamlitから任意のファイル変更監視を高速化するWatchdogの追加案内が出た。

## 原因

- 前者2件の通信問題とポート問題は、作業環境のネットワーク・ソケット制限によるものだった。
- urllib3警告はApple標準PythonがOpenSSLではなく古いLibreSSLでビルドされているためだった。
- Watchdogは必須依存ではなく、未導入時に表示される性能向上の案内だった。

## 解決方法

- 利用者の許可を得たネットワーク環境で依存関係と実データを取得した。
- Python 3.9環境向けに `urllib3==1.26.20` を固定し、警告が消えることを確認した。
- 許可された環境でStreamlitを起動し、ヘルスチェック後に正常終了した。
- Watchdogは機能に影響しないため、第1段階の必須依存には追加しなかった。

## 動作確認結果

- `.venv` をPython 3.9.6で作成し、全依存ライブラリをインストールできた。
- 主要モジュールのimportに成功した。
- pytest: 5件すべて成功、警告なし。
- 7203.Tの実データ: 244件（2025-07-22〜2026-07-22）を取得した。
- 列: Adj Close、Close、High、Low、Open、Volume、MA_20、MA_50を確認した。
- Plotly Figure: 終値、20日移動平均線、50日移動平均線の3系列を確認した。
- Streamlit: `http://localhost:8501` で起動し、ヘルスチェックが `ok`、ルートHTMLも正常応答した。確認後に停止した。
- 空データ・通信失敗時は `StockDataError` となり、強制終了を避けるUI経路があることを単体テストと実通信失敗の両方で確認した。

## 学んだこと

- 外部APIの結果は正常、空、列構造の違い、通信例外を分けて扱う必要がある。
- UIとデータ処理を分離すると、通信なしの単体テストを書きやすい。

## 次に行うこと

第1段階は完了。次に進む際は、未来情報の混入を防ぎながら特徴量作成、目的変数、時系列分割を実装する。

---

# Phase 2: Training Data

## 日付

2026-07-23

## 実装した内容

- Daily Return、5日・20日リターンを実装
- MA5、MA20、MA50とMA20乖離率を実装
- Daily Returnの20日ローリング標準偏差として20日ボラティリティを実装
- 出来高の前営業日比を実装
- 翌営業日の終値が当日より高い場合を1、それ以外を0とするTargetを実装
- 特徴量とTargetがそろった行だけを残す学習用DataFrame作成処理を実装
- 将来データを変更しても過去の特徴量が変化しないことを自動テストへ追加

## 発生した問題

- 最終行は翌営業日終値が存在しないため、単純な比較では誤って0になる可能性がある。
- 移動平均などのrolling計算では、計算期間に満たない先頭行にNaNが生じる。

## 原因

- pandasでは欠損値との大小比較がFalseになるため、翌日不明と非上昇を区別する必要がある。
- MA50には当日を含む50営業日分の履歴が必要であり、先頭49行では計算できない。

## 解決方法

- Targetをnullable整数として作り、当日終値と翌日終値が両方ある行だけ0または1を設定した。
- `dropna` の対象を特徴量列とTarget列だけに限定し、学習に無関係な列の欠損では行を削除しないようにした。
- 0除算で生じる無限値は特徴量列だけNaNへ変換し、同じ欠損処理へ統合した。

## 動作確認結果

- pytest: 新規4件を含む9件すべて成功。
- Targetの1・0・最終行の欠損を確認した。
- 将来70営業日目以降の価格・出来高を変更しても、それ以前の全特徴量が同一であることを確認した。
- 学習用データではTargetに欠損がなく、学習に無関係な列の欠損だけでは行を削除しないことを確認した。

## 学んだこと

- 目的変数の最終行を0にすると、実際には不明な結果を下落として学習させてしまう。
- 欠損値は一括削除せず、発生理由と学習に必要な列を考えて対象を限定する必要がある。
- 将来情報の不使用は、将来部分を変更して過去特徴量が不変かを比較するとテストできる。

## 次に行うこと

次段階では日付順を維持した学習・テスト分割とモデル評価を設計する。LightGBM、バックテスト、UI変更はこの段階では行わない。
